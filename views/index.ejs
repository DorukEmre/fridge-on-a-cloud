<%- include('partials/head.ejs') %>
    <title>Fridge on a cloud</title>
</head>
<body>
    <%- include('partials/sidebar.ejs') %>
    <h1><%= title %></h1>
    <p>Keep all the ingredients in your fridge fresh</p>
    <h2>What's in my fridge?</h2>
    <% if (error) { %>
        <p>Error getting dynamic content.</p>
    <<% } else { %>
        <ul>
            <li><a href="/product">Products</a>: <%= data.product_count %></li>
            <li><a href="/category">Categories</a>: <%= data.category_count %></li>
        </ul>
    <% } %>

<%- include('partials/footer.ejs') %>